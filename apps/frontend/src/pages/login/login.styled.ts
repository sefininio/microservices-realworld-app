import styled from 'styled-components';
import { StyledGrayLink, StyledButtonBar } from '../../components/components.styled';

const StyledRegisterLink = styled(StyledGrayLink)`
`;

export { StyledRegisterLink, StyledButtonBar };
