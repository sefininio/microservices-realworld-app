import styled from 'styled-components';
import { StyledGrayLink, StyledButtonBar } from '../../components/components.styled';

const StyledLoginLink = styled(StyledGrayLink)`
`;

export {
  StyledLoginLink,
  StyledButtonBar,
};
